# Holdout dataset

Only independent captures that were never used for templates or threshold tuning belong here.
Until labelled frames are added, `verifyHoldout` prints `NOT_RUN`; it must never be presented as a pass.
